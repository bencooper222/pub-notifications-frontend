{
  "name": "pub-notifications",
  "description": "Frontend for receiving texts to phone when order is up at the pub",
  "version": "1.0.0",
  "author": "Benjamin Cooper",
  "private": true,
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html",
    "lint": "prettier index.html style.scss main.js --write"
  },
  "dependencies": {
    "parcel-bundler": "^1.9.0"
  },
  "devDependencies": {
    "node-sass": "^4.5.0",
    "prettier": "^1.15.3"
  }
}
